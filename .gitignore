# Jekyll
_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata

# macOS
.DS_Store

# Editor files
.vscode/
.idea/
*.swp
*.swo

# Temporary files
*.tmp
*.temp

# Workspace
workspace/

# Local scratch directory (large PDFs, review archives, ephemera)
tmp/

# Per-machine Claude Code permissions (accumulates noisily across sessions)
.claude/settings.local.json

# Private PDFs
pdf/internal/

# Bulk page-image extracts (large; keep locally only)
assets/external/catholic-protectory-1909/

# Raw Downloads-ancestry material filed per the 2026-06-11 inventory (large; keep locally only)
assets/external/ancestry/james-higgins/
assets/external/_namesakes-not-ours/
assets/external/*.ged

# High-res original images (keep locally, don't publish)
images/originals/

# Only JPGs in web image directories (PNGs stay in originals/)
images/bio/*.png
images/locations/*.png
images/thumbs/*.png
